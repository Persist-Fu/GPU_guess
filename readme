correstness.cu
编译指令：nvcc correctness_guess.cu train.cpp guessing.cu md5.cpp -o main_gpu -O2
编译后执行指令 ./main_gpu

main.cu
启用O2优化的编译指令：nvcc main.cu train.cpp guessing.cu md5.cpp -o main_gpu -O2
编译后执行指令 ./main_gpu
执行完编译与测试脚本指令后可得性能测试结果
